# 用户页精简无效统计并调整额度位置

## 本次调整

- 去掉 `/user` 页面顶部无效统计卡片，不再展示阅读、点赞、评论、转发、收藏等拿不到的指标
- 去掉“账号概览”模块，页面只保留平台筛选、文章列表和充值能力
- 免费额度、付费额度移动到右上角用户名旁边展示，减少中部重复信息
- 保留充值入口，但移动到顶部操作区，避免页面结构松散
- 用户页数据加载不再请求 `/api/user/stats` 和 `/api/accounts`，只拉取实际在页面使用的数据
- 文章列表移除阅读/点赞/评论/转发列，只保留标题、平台、账号、Token、发布时间

## 校验结果

- `./node_modules/.bin/tsc --noEmit`：通过
- `npm run build`：失败，仍然是本机 Node 18 环境下 `undici` 缺少全局 `File`，不是本次改动引起
