# 定时任务 - AI 选择话题改为返回 URL 避免序号错位

## 修改时间
2026-03-15 16:30

## 问题描述
AI 选择话题时返回的是序号（如 index: 29, 40），但实际生成文章时使用的话题和 AI 选择的不一致。这是因为序号可能对不上，导致选错话题。

## 问题分析
原来的流程：
1. 抓取热榜，得到文章列表（包含 title 和 url）
2. 将列表发给 AI，格式：`1. 标题`
3. AI 返回序号：`{"selections": [{"index": 29, "reason": "..."}, {"index": 40, "reason": "..."}]}`
4. 根据序号从列表中提取文章：`articles[index - 1]`

问题：如果列表在传递过程中有任何变化（如过滤、排序），序号就会对不上。

## 解决方案
让 AI 直接返回话题的完整 URL，而不是序号：

1. 将列表发给 AI 时，同时包含标题和 URL：`1. 标题 | URL`
2. AI 返回完整 URL：`{"selections": [{"url": "https://...", "reason": "..."}, ...]}`
3. 根据 URL 在列表中查找匹配的文章：`articles.find(a => a.url === sel.url)`

这样可以确保 AI 选择的话题和实际使用的话题完全一致。

## 修改内容

### 1. 修改热榜列表格式
```typescript
// 修改前：只包含标题
const articleList = articles.slice(0, 50).map((article, index) => 
  `${index + 1}. ${article.title}`
).join('\n');

// 修改后：同时包含标题和 URL
const articleList = articles.slice(0, 50).map((article, index) => 
  `${index + 1}. ${article.title} | ${article.url}`
).join('\n');
```

### 2. 修改 AI 提示词
```typescript
// 系统提示词
const systemPrompt = `你是一个专业的内容选题助手。用户会给你一个热榜列表和选题要求，你需要从中选择最合适的话题。

要求：
1. 严格按照用户的选题要求进行筛选
2. 优先选择热度高、有讨论价值的话题
3. 避免重复或相似的话题
4. 返回格式必须是 JSON 对象，包含选中话题的完整 URL 和选择理由

返回格式示例：
{
  "selections": [
    {"url": "https://example.com/article1", "reason": "该话题讨论度高，符合科技类要求"},
    {"url": "https://example.com/article2", "reason": "热点事件，具有时效性"}
  ]
}`;

// 用户提示词
const userPrompt = `请从以下热榜中选择 ${articleCount} 个最合适的话题：

${articleList}

${task.customPrompt ? `\n选题要求：${task.customPrompt}\n` : '\n选题要求：选择热度高、有讨论价值的话题\n'}

请返回 JSON 格式的选择结果，包含每个话题的完整 URL 和选择理由。注意：必须返回列表中的原始 URL，不要修改。`;
```

### 3. 修改解析逻辑
```typescript
// 解析 AI 返回的选择结果
let selections: Array<{ url: string; reason: string }> = [];
try {
  // 尝试提取 JSON（可能包含在 markdown 代码块中）
  let jsonText = aiResponse;
  const jsonMatch = aiResponse.match(/```(?:json)?\s*(\{[\s\S]*?\})\s*```/);
  if (jsonMatch) {
    jsonText = jsonMatch[1];
  }
  
  const parsed = JSON.parse(jsonText);
  if (parsed.selections && Array.isArray(parsed.selections)) {
    selections = parsed.selections;
  } else if (Array.isArray(parsed)) {
    // 兼容直接返回数组的格式
    selections = parsed;
  }
} catch (e) {
  await taskLog(task.id, 'error', `❌ JSON 解析失败: ${e}`);
}

if (selections.length === 0) {
  throw new Error('AI 未返回有效的选择结果');
}

// 根据 URL 匹配文章
const selectedArticles = selections
  .map(sel => {
    // 在原始列表中查找匹配的 URL
    const article = articles.find(a => a.url === sel.url);
    return article ? { ...article, reason: sel.reason } : null;
  })
  .filter((article): article is { title: string; url: string; reason: string } => article !== null)
  .slice(0, articleCount); // 确保不超过请求数量

if (selectedArticles.length === 0) {
  throw new Error('AI 返回的 URL 无法匹配到热榜文章');
}
```

## 优势
1. 精确匹配：URL 是唯一标识，不会出现序号错位问题
2. 更健壮：即使列表顺序变化，也能正确匹配
3. 易于调试：日志中可以直接看到 AI 选择的 URL，方便核对

## 修改文件
- `src/background/scheduler.ts` - `selectArticlesWithAI` 函数

## 测试建议
1. 运行定时任务，查看日志中 AI 返回的 URL
2. 确认实际打开的页面 URL 和 AI 返回的 URL 一致
3. 测试多篇文章生成，确保每篇都是 AI 选择的话题
