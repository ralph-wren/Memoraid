# 额度显示：修复生成文章后额度不更新问题

## 修改日期
2026-03-12 14:30

## 问题描述
用户反馈：使用付费额度生成新文章后，插件首页显示的剩余额度不会扣减，数字保持不变。

## 问题原因
1. 前端使用了5分钟的额度缓存机制，避免频繁请求服务器
2. 生成文章完成后，虽然清除了缓存，但组件不会自动重新加载额度信息
3. 只有关闭并重新打开插件popup时，才会重新获取最新额度
4. 这导致用户在同一次打开popup期间，看到的额度数据一直是旧的

## 解决方案
在任务完成时，不仅清除缓存，还要立即重新加载额度信息（强制刷新）。

## 修改内容

### 前端修改（src/components/Home.tsx）

#### 1. 将 loadQuota 函数提取到 useEffect 外部
```typescript
// 加载用户额度信息 - 使用缓存机制
const loadQuota = async (forceRefresh = false) => {
  try {
    // 如果不是强制刷新，先尝试从缓存读取
    if (!forceRefresh) {
      const cached = await chrome.storage.local.get(['quotaCache', 'quotaCacheTime']);
      const now = Date.now();
      const cacheExpiry = 5 * 60 * 1000; // 5分钟缓存有效期
      
      // 如果缓存存在且未过期，直接使用缓存
      if (cached.quotaCache && cached.quotaCacheTime && (now - cached.quotaCacheTime < cacheExpiry)) {
        setQuota(cached.quotaCache);
        return;
      }
    }
    
    // 缓存不存在、已过期或强制刷新，从服务器获取
    const settings = await getSettings();
    const backendUrl = settings.sync?.backendUrl || 'https://memoraid.dpdns.org';
    const token = settings.sync?.token;
    const anonymousId = settings.anonymousId;
    
    // 构建请求头
    const headers: Record<string, string> = {
      'Content-Type': 'application/json'
    };
    
    // 优先使用 token，如果没有则使用匿名 ID
    if (token) {
      headers['Authorization'] = `Bearer ${token}`;
    } else if (anonymousId) {
      headers['X-Anonymous-ID'] = anonymousId;
    }
    
    const response = await fetch(`${backendUrl}/api/user/quota`, {
      method: 'GET',
      headers
    });
    
    if (response.ok) {
      const data = await response.json();
      setQuota(data);
      // 保存到缓存
      const now = Date.now();
      await chrome.storage.local.set({
        quotaCache: data,
        quotaCacheTime: now
      });
    } else {
      // 加载失败，设置为 null
      setQuota(null);
    }
  } catch (error) {
    console.error('Failed to load quota:', error);
    // 加载失败，设置为 null
    setQuota(null);
  }
};
```

**说明**：
- 添加 `forceRefresh` 参数，支持强制刷新（跳过缓存）
- 将函数提取到 useEffect 外部，使其可以在其他地方调用

#### 2. 在任务完成时重新加载额度
```typescript
// 如果任务完成，清除额度缓存并重新加载额度信息
if (isDone) {
  chrome.storage.local.remove(['quotaCache', 'quotaCacheTime']);
  // 重新加载额度信息（强制刷新）
  loadQuota(true);
}
```

**说明**：
- 当任务状态为 `Done!` 或 `Refined!` 时
- 清除本地缓存
- 调用 `loadQuota(true)` 强制从服务器获取最新额度
- 用户无需关闭重开popup，即可看到最新额度

## 技术细节

### 缓存机制
- 正常情况：使用5分钟缓存，减少服务器请求
- 强制刷新：跳过缓存检查，直接从服务器获取最新数据
- 缓存存储：使用 `chrome.storage.local` 存储额度数据和时间戳

### 触发时机
- 文章生成完成（`Done!`）
- 文章优化完成（`Refined!`）
- 这两种情况都会消耗额度，需要立即更新显示

### 用户体验改进
- 之前：生成文章后，额度显示不变，需要关闭重开popup才能看到最新额度
- 现在：生成文章后，额度自动更新，实时显示最新剩余额度
- 避免用户误以为额度没有扣减或系统出错

## 部署记录

### 前端编译
```bash
npm run build
```
✅ 编译成功

### 部署步骤
1. 前端已编译到 `dist` 目录
2. 需要在 Chrome 扩展管理页面重新加载扩展
3. 或者等待 Cloudflare Pages 自动部署

## 验证建议
1. 打开插件popup，查看当前剩余额度
2. 生成一篇新文章（消耗1次额度）
3. 文章生成完成后，观察额度显示
4. 确认额度数字自动减1，无需关闭重开popup

## 相关功能
- 额度显示：插件首页显示剩余额度和充值按钮
- 额度扣减：生成文章时自动扣减额度
- 缓存机制：减少服务器请求，提升响应速度
