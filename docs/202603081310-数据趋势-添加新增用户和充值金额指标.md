# 数据趋势添加新增用户和充值金额指标

## 功能描述
在管理员页面的仪表盘数据趋势区域，新增了两个关键指标的图表展示：
- 🆕 每日新增用户
- 💰 每日充值金额

## 实现内容

### 1. 前端HTML结构调整
**文件：** `backend/src/index.ts`

将原来的单行2列布局改为两行2列布局：
- 第一行：活跃用户 + 生成文章
- 第二行：新增用户 + 充值金额

```html
<!-- 第一行：活跃用户 + 生成文章 -->
<div style="display:grid;grid-template-columns:1fr 1fr;gap:24px;margin-bottom:24px">
    <div class="card" style="padding:20px">
        <h3>👥 每日活跃用户</h3>
        <canvas id="dashActiveChart"></canvas>
    </div>
    <div class="card" style="padding:20px">
        <h3>📝 每日生成文章</h3>
        <canvas id="dashArticlesChart"></canvas>
    </div>
</div>

<!-- 第二行：新增用户 + 充值金额 -->
<div style="display:grid;grid-template-columns:1fr 1fr;gap:24px">
    <div class="card" style="padding:20px">
        <h3>🆕 每日新增用户</h3>
        <canvas id="dashNewUsersChart"></canvas>
    </div>
    <div class="card" style="padding:20px">
        <h3>💰 每日充值金额</h3>
        <canvas id="dashRechargeChart"></canvas>
    </div>
</div>
```

### 2. 数据加载函数更新
**函数：** `loadDashboardCharts()`

从加载2个图表扩展为加载4个图表，并行请求API获取数据：

```javascript
async function loadDashboardCharts() {
    const token = localStorage.getItem('memoraid_admin_token');
    const days = currentChartRange;
    try {
        // 并行请求四个趋势数据
        const [activeRes, articlesRes, newUsersRes, rechargeRes] = await Promise.all([
            fetch('/api/admin/trends?type=activeUsers&days=' + days, { headers: { 'Authorization': 'Bearer ' + token } }),
            fetch('/api/admin/trends?type=newArticles&days=' + days, { headers: { 'Authorization': 'Bearer ' + token } }),
            fetch('/api/admin/trends?type=newUsers&days=' + days, { headers: { 'Authorization': 'Bearer ' + token } }),
            fetch('/api/admin/trends?type=rechargeAmount&days=' + days, { headers: { 'Authorization': 'Bearer ' + token } })
        ]);
        
        const activeData = await activeRes.json();
        const articlesData = await articlesRes.json();
        const newUsersData = await newUsersRes.json();
        const rechargeData = await rechargeRes.json();
        
        // 绘制四个图表，使用不同颜色
        drawDashChart('dashActiveChart', activeData.labels, activeData.values, '#10b981'); // 绿色 - 活跃用户
        drawDashChart('dashArticlesChart', articlesData.labels, articlesData.values, '#fbbf24'); // 黄色 - 生成文章
        drawDashChart('dashNewUsersChart', newUsersData.labels, newUsersData.values, '#3b82f6'); // 蓝色 - 新增用户
        drawDashChart('dashRechargeChart', rechargeData.labels, rechargeData.values, '#8b5cf6'); // 紫色 - 充值金额
    } catch (e) {
        console.error('仪表盘图表加载失败:', e);
    }
}
```

### 3. 图表颜色方案
- 🟢 活跃用户：`#10b981` (绿色)
- 🟡 生成文章：`#fbbf24` (黄色)
- 🔵 新增用户：`#3b82f6` (蓝色)
- 🟣 充值金额：`#8b5cf6` (紫色)

## 后端API支持
后端API `/api/admin/trends` 已经支持以下类型：
- `activeUsers` - 每日活跃用户数
- `newArticles` - 每日生成文章数
- `newUsers` - 每日新增用户数 ✨ 新增
- `rechargeAmount` - 每日充值金额 ✨ 新增

## 数据统计逻辑

### 新增用户统计
从 `users` 表统计每天新注册的用户数：
```sql
SELECT ((created_at + 28800) / 86400) as day_key, COUNT(*) as cnt
FROM users
WHERE created_at >= ?
GROUP BY day_key
```

### 充值金额统计
从 `payment_orders` 表统计每天已支付订单的金额总和：
```sql
SELECT ((paid_at + 28800) / 86400) as day_key, SUM(amount) as total
FROM payment_orders
WHERE paid_at >= ? AND status = 'paid'
GROUP BY day_key
```

## 功能特性
- ✅ 支持切换时间范围：近7天、近30天、近90天
- ✅ 使用Canvas绘制折线图，包含渐变填充
- ✅ 自动补齐无数据的日期（显示为0）
- ✅ 并行请求API，提升加载速度
- ✅ 切换标签页时自动刷新数据

## 部署说明
修改完成后，使用以下命令部署到 Cloudflare Workers：
```bash
cd backend
npx wrangler deploy
```

## 相关文件
- `backend/src/index.ts` - 管理员页面主文件
  - HTML结构：第4370-4420行
  - 数据加载函数：第5053-5075行
