# 订单审核通过发送邮件通知

## 更新时间
2026年3月5日 22:45

## 功能描述
当管理员审核充值订单通过时,系统自动向用户发送格式化的充值成功邮件通知,包含充值信息和当前账户额度详情。

## 实现内容

### 1. 邮件发送逻辑

在订单审核通过后,添加以下步骤:

1. 查询用户当前额度信息(免费额度+付费额度)
2. 获取用户邮箱地址
3. 获取系统邮件配置(发件人、发件人名称)
4. 发送格式化的HTML邮件

### 2. 邮件内容

#### 邮件主题
```
🎉 充值成功通知 - Memoraid
```

#### 邮件内容包含

**充值信息卡片**:
- 订单号
- 充值金额(突出显示,绿色)
- 增加额度(突出显示,绿色)

**当前账户额度卡片**(渐变绿色背景):
- 免费额度: X 次
- 付费额度: X 次
- 总额度: X 次(加粗显示)

**操作按钮**:
- "前往内容中心"按钮,链接到用户中心

### 3. 邮件样式特点

1. **响应式设计**: 适配各种邮件客户端
2. **渐变背景**: 头部使用绿色渐变(#10b981 → #059669)
3. **卡片布局**: 信息分块展示,清晰易读
4. **颜色编码**:
   - 充值金额/额度: 绿色(#10b981)
   - 总额度: 深绿色(#047857)
   - 普通文本: 灰色系
5. **阴影效果**: 按钮和卡片有适当阴影
6. **品牌一致性**: 与系统UI风格保持一致

### 4. 代码实现

```typescript
// 获取用户当前额度信息
const userQuota = await env.DB.prepare(`
    SELECT 
        COALESCE(free_quota_remaining, 0) as free_quota,
        COALESCE(paid_quota_remaining, 0) as paid_quota
    FROM user_quotas 
    WHERE user_id = ?
`).bind(order.user_id).first();

const freeQuota = userQuota?.free_quota || 0;
const paidQuota = userQuota?.paid_quota || 0;
const totalQuota = freeQuota + paidQuota;

// 获取用户邮箱
const user = await env.DB.prepare('SELECT email FROM users WHERE id = ?').bind(order.user_id).first();

// 发送充值成功邮件通知
if (user && user.email && env.RESEND_API_KEY) {
    try {
        // 获取邮件配置
        const configs = await env.DB.prepare('SELECT key, value FROM system_config WHERE key IN (?, ?)').bind('email_sender', 'email_sender_name').all();
        const configMap: Record<string, string> = {};
        configs.results.forEach((row: any) => configMap[row.key] = row.value);
        
        const emailSender = configMap.email_sender || 'onboarding@resend.dev';
        const emailSenderName = configMap.email_sender_name || 'Memoraid';
        
        // 发送邮件
        await sendEmailViaResend(env.RESEND_API_KEY, {
            from: emailSender,
            fromName: emailSenderName,
            to: user.email as string,
            subject: '🎉 充值成功通知 - Memoraid',
            text: '...',  // 纯文本版本
            html: '...'   // HTML版本
        });
        
        console.log('充值成功邮件已发送至:', user.email);
    } catch (emailError) {
        console.error('发送充值成功邮件失败:', emailError);
        // 邮件发送失败不影响审批流程
    }
}
```

### 5. 错误处理

- 邮件发送失败不会影响订单审批流程
- 失败信息会记录到控制台日志
- 审批页面会显示"邮件通知已发送"(即使发送失败,审批仍然成功)

## 邮件模板预览

### 头部
```
🎉 充值成功
您的账户额度已成功充值
```

### 充值信息
```
📋 充值信息
订单号: order_xxx
充值金额: ¥100
增加额度: +50 次
```

### 当前账户额度
```
💰 当前账户额度
免费额度: 20 次
付费额度: 50 次
总额度: 70 次
```

### 操作按钮
```
[前往内容中心]
```

### 页脚
```
此邮件由 Memoraid 系统自动发送，请勿直接回复
© 2026 Memoraid. All rights reserved.
```

## 技术要点

1. **使用Resend服务**: 通过Resend API发送邮件
2. **HTML邮件**: 使用table布局确保兼容性
3. **内联样式**: 所有样式都使用内联style,确保邮件客户端正确显示
4. **渐变背景**: 使用linear-gradient创建视觉效果
5. **响应式**: 固定宽度600px,适配大多数邮件客户端
6. **纯文本备份**: 提供text版本,兼容不支持HTML的客户端

## 部署信息

- **后端版本ID**: 692d642c-0ba3-4a69-988c-96057cc4b90b
- **部署时间**: 2026年3月5日 22:45
- **部署命令**: `npx wrangler deploy` (在backend目录)

## 测试建议

1. 审核一个测试订单
2. 检查用户邮箱是否收到邮件
3. 验证邮件内容格式是否正确
4. 确认额度信息是否准确
5. 测试"前往内容中心"按钮链接

## 注意事项

- 需要配置RESEND_API_KEY环境变量
- 需要在系统配置中设置发件人邮箱
- 邮件发送是异步的,不会阻塞审批流程
- 建议使用已验证的域名邮箱作为发件人
- 测试时可使用 onboarding@resend.dev

## 相关文档

- `docs/202602261305-邮件发送-修复Cloudflare-Workers邮件功能.md`
- `docs/202602261320-邮件审批-实现一键审批充值订单.md`
