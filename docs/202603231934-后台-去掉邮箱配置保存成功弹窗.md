# 后台去掉邮箱配置保存成功弹窗

## 本次调整

- 管理后台邮件配置保存成功后不再弹出“配置保存成功”提示
- 保存成功时改为静默完成，避免频繁修改配置时被弹窗打断
- 保存失败和网络错误提示仍然保留，避免问题被静默吞掉

## 校验结果

- `./node_modules/.bin/tsc --noEmit`：通过
- `npm run build`：失败，仍然是本机 Node 18 环境下 `undici` 缺少全局 `File`，不是本次改动引起
